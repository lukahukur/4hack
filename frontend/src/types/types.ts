export type Topics = { id: number; name: string; count: number }
export type postPreviewType = {
  author: string
  c_date: string
  header: string
  image: string
  metatags: string
  topic: string
  id: number
}
